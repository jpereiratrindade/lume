[Service]
# This machine exposes legacy Intel/AMD Vulkan devices that Ollama detects but
# cannot use. Keep Lume local and predictable by selecting the AVX2 CPU runner.
Environment="OLLAMA_LLM_LIBRARY=cpu_avx2"
Environment="OLLAMA_VULKAN=0"

